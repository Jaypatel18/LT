import json
from pprint import pprint

with open('PhotoID.json') as data_file:    
    data = json.load(data_file)

pprint(albumId) #print the number of the ID
pprint(id) #print the id of the text file
pprint(title) #print the title of the text file
pprint(title) #print the next title of the same number
